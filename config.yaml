baseURL: "https://sre.run/"
languageCode: "en-us"
title: "Prometheus Alerts"
dataDir: rules
assetDir: rules

theme: book

params:
  BookRepo: "https://github.com/sre/prometheus-alerts"
  BookEditPath: "edit/main"
  BookSearch: true
  BookSection: "runbooks"
  #BookMenuBundle: "/menu"


markup:
  goldmark:
    renderer:
      # Allows rendering HTML tags. This would only be truly "unsafe" if we were accepting untrusted user input and rendering it, which we aren't.
      unsafe: true

#module:
#  imports:
#    - path: "github.com/google/docsy"
